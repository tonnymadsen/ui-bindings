package com.rcpcompany.uibindings.navigator.editors.internal.handlers;

import org.eclipse.core.commands.AbstractHandler;
import org.eclipse.core.commands.ExecutionEvent;
import org.eclipse.core.commands.ExecutionException;
import org.eclipse.core.commands.IHandler;

/**
 * Handler fopr the "Pin Editor" command.
 * 
 * @author Tonny Madsen, The RCP Company
 */
public class PinEditorHandler extends AbstractHandler implements IHandler {

	@Override
	public Object execute(ExecutionEvent event) throws ExecutionException {
		// TODO Update the state of the view
		return null;
	}
}
